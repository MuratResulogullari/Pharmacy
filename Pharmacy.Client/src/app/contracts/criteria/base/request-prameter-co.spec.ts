import { RequestPrameterCO } from './request-prameter-co';

describe('RequestPrameterCO', () => {
  it('should create an instance', () => {
    expect(new RequestPrameterCO()).toBeTruthy();
  });
});
